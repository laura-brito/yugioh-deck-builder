<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base para o modo escuro -->
    <style name="Base.Theme.YuGiOhDeckBuilder" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/purple_primary</item>
        <item name="colorSecondary">@color/pink_secondary</item>
        <item name="android:textColor">@color/blue_text_main</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="actionBarStyle">@style/CustomActionBarTheme</item>
        <item name="alertDialogTheme">@style/CustomAlertDialogTheme.Dark</item>
    </style>

    <!-- Estilo para os diálogos em modo escuro -->
    <style name="CustomAlertDialogTheme.Dark" parent="ThemeOverlay.Material3.Dialog.Alert">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="materialAlertDialogTitleTextStyle">@style/DialogTitleTextStyle.Dark</item>
    </style>

    <style name="DialogTitleTextStyle.Dark" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>